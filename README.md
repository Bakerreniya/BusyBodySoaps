
<html lang="en" class="no-js">
<head>
    <!-- ULTRA SEO + SOCIAL TAGS -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0">
    <meta name="description" content="Handcrafted soap art - Natural, clean, and thoughtfully crafted soaps made with only the finest ingredients.">
    <meta name="keywords" content="handcrafted soap, natural soap, artisan soap, aloe vera soap, lavender soap, turmeric soap, bamboo charcoal soap">
    <meta name="author" content="Busy Body Soaps">
    <link rel="canonical" href="https://busybodyshop.com">
    
    <!-- OPEN GRAPH - FOR SICK SOCIAL SHARES -->
    <meta property="og:title" content="Busy Body Soaps - Handcrafted Soap Art">
    <meta property="og:description" content="Natural, clean, and thoughtfully crafted soaps made with only the finest ingredients.">
    <meta property="og:image" content="https://www.dropbox.com/scl/fi/q7wp1xkc7215n80aejgrq/IMG_7314.jpeg?rlkey=41kcft32lvluzs7hzhxxo40zu&st=e5s65iff&dl=0">
    <meta property="og:url" content="https://busybodyshop.com">
    <meta property="og:type" content="website">
    <meta property="og:site_name" content="Busy Body Soaps">
    
    <!-- TWITTER CARDS - FOR MAXIMUM RADNESS -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="Busy Body Soaps - Handcrafted Soap Art">
    <meta name="twitter:description" content="Natural, clean, and thoughtfully crafted soaps made with only the finest ingredients.">
    <meta name="twitter:image" content="https://www.dropbox.com/scl/fi/q7wp1xkc7215n80aejgrq/IMG_7314.jpeg?rlkey=41kcft32lvluzs7hzhxxo40zu&st=e5s65iff&dl=0">
    
    <!-- FAVICON + APPLE TOUCH -->
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🧼</text></svg>">
    <link rel="apple-touch-icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🧼</text></svg>">
    <meta name="theme-color" content="#0A0E2A">
    
    <!-- SCHEMA MARKUP FOR GOOGLE -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "LocalBusiness",
      "name": "Busy Body Soaps",
      "description": "Handcrafted soap art - Natural, clean, and thoughtfully crafted soaps made with only the finest ingredients.",
      "url": "https://busybodyshop.com",
      "telephone": "(215) 651-3763",
      "address": {
        "@type": "PostalAddress",
        "streetAddress": "Mebi Kli Lopera Rd"
      },
      "image": "https://www.dropbox.com/scl/fi/q7wp1xkc7215n80aejgrq/IMG_7314.jpeg?rlkey=41kcft32lvluzs7hzhxxo40zu&st=e5s65iff&dl=0"
    }
    </script>
    
    <title>Busy Body Soaps - Handcrafted Soap Art</title>
    
    <!-- FONT AWESOME + GOOGLE FONTS - ULTRA TECHY -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;600;700;800;900&family=Rajdhani:wght@300;400;500;600;700&family=Share+Tech+Mono&family=Exo+2:wght@300;400;500;600;700;800;900&family=Audiowide&family=Geo:ital@0;1&family=Monoton&family=Wallpoet&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        /* === CSS RESET - CLEAN EDITION === */
        *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
        html { scroll-behavior: smooth; scroll-padding-top: 80px; }
        body { line-height: 1.6; font-size: 16px; min-height: 100vh; overflow-x: hidden; }
        img, video { max-width: 100%; height: auto; display: block; }
        button, input, textarea { font-family: inherit; }
        :focus { outline: 2px solid #FF4A24; outline-offset: 3px; }
        :focus:not(:focus-visible) { outline: none; }
        
        /* === ORIGINAL COLOR SYSTEM - WITH TECH FONTS === */
        :root {
            /* PRIMARY COLORS - ORIGINAL NEON */
            --neon-orange: #FF4A24;       /* Neon Orange-Red */
            --hot-red: #E8321A;           /* Rich Hot Red */
            --electric-blue: #46B8FF;     /* Electric Blue */
            --aqua-blue: #00D4FF;         /* Bright Aqua for accents */
            
            /* BACKGROUND COLORS - ORIGINAL NAVY */
            --deep-navy: #0A0E2A;         /* Deep Navy - Main Background */
            --dark-navy: #0C112E;         /* Dark Navy - Slightly lighter */
            --navy-card: #111535;         /* Navy for cards */
            --navy-border: #1A1F44;       /* Navy border */
            --navy-hover: #13183C;        /* Navy hover state */
            
            /* TEXT COLORS */
            --text-white: #FFFFFF;
            --text-light: #E8EAFF;
            --text-muted: #B0B5D0;
            --text-glow: 0 0 10px currentColor;
            
            /* GLOW EFFECTS - ORIGINAL ENHANCED */
            --glow-orange: 0 0 20px rgba(255, 74, 36, 0.8), 0 0 40px rgba(255, 74, 36, 0.4);
            --glow-blue: 0 0 20px rgba(70, 184, 255, 0.8), 0 0 40px rgba(70, 184, 255, 0.4);
            --glow-aqua: 0 0 20px rgba(0, 212, 255, 0.8), 0 0 40px rgba(0, 212, 255, 0.4);
            
            /* ANIMATIONS */
            --transition-fast: 0.2s cubic-bezier(0.4, 0, 0.2, 1);
            --transition-medium: 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            --transition-slow: 0.6s cubic-bezier(0.4, 0, 0.2, 1);
            
            /* SHADOWS */
            --shadow-strong: 0 10px 30px rgba(0, 0, 0, 0.7);
            --shadow-hover: 0 15px 40px rgba(0, 0, 0, 0.8);
        }
        
        /* CONTAINER FIX */
        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 20px;
            width: 100%;
        }
        
        /* === DARK NAVY BACKGROUND WITH TECH PATTERN === */
        body {
            font-family: 'Share Tech Mono', 'Rajdhani', monospace;
            background-color: var(--deep-navy);
            color: var(--text-white);
            position: relative;
            min-height: 100vh;
            overflow-x: hidden;
        }
        
        /* TECH CIRCLE PATTERN */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                radial-gradient(circle at 20% 30%, rgba(255, 74, 36, 0.05) 0%, transparent 25%),
                radial-gradient(circle at 80% 70%, rgba(70, 184, 255, 0.05) 0%, transparent 25%),
                radial-gradient(circle at 40% 80%, rgba(0, 212, 255, 0.03) 0%, transparent 20%);
            pointer-events: none;
            z-index: -2;
        }
        
        /* FLOATING GRID PATTERN */
        body::after {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                linear-gradient(90deg, transparent 99%, rgba(70, 184, 255, 0.03) 100%),
                linear-gradient(0deg, transparent 99%, rgba(70, 184, 255, 0.03) 100%);
            background-size: 40px 40px;
            animation: gridFloat 40s linear infinite;
            pointer-events: none;
            z-index: -1;
            opacity: 0.3;
        }
        
        @keyframes gridFloat {
            0% { background-position: 0 0; }
            100% { background-position: 40px 40px; }
        }
        
        /* HEADINGS - ULTRA TECHY FONTS WITH ORIGINAL COLORS */
        h1, h2, h3, h4, h5, h6 {
            font-family: 'Orbitron', 'Audiowide', 'Wallpoet', sans-serif;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 3px;
            position: relative;
        }
        
        h1 { 
            color: var(--neon-orange);
            text-shadow: var(--glow-orange);
        }
        
        h2 { 
            color: var(--electric-blue);
            text-shadow: var(--glow-blue);
        }
        
        h3 { 
            color: var(--aqua-blue);
            text-shadow: var(--glow-aqua);
        }
        
        /* === HEADER - TECH MINIMAL === */
        header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background-color: rgba(10, 14, 42, 0.95);
            backdrop-filter: blur(15px);
            border-bottom: 2px solid var(--neon-orange);
            box-shadow: var(--glow-orange);
            z-index: 9999;
            padding: 15px 0;
            transition: var(--transition-medium);
        }
        
        .header-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 20px;
        }
        
        /* === TECH LOGO WITH ORIGINAL COLORS === */
        .logo-container {
            position: relative;
            cursor: pointer;
        }
        
        .logo-text {
            font-family: 'Wallpoet', 'Audiowide', 'Orbitron', sans-serif;
            font-size: 32px;
            font-weight: 900;
            color: var(--neon-orange);
            position: relative;
            letter-spacing: 2px;
            text-transform: uppercase;
            animation: 
                techPulse 3s ease-in-out infinite,
                digitalShake 10s ease-in-out infinite;
            text-shadow: var(--glow-orange);
        }
        
        /* TECH PULSE ANIMATION */
        @keyframes techPulse {
            0%, 100% { 
                text-shadow: var(--glow-orange);
            }
            50% { 
                text-shadow: 
                    0 0 20px var(--neon-orange),
                    0 0 40px var(--neon-orange),
                    0 0 60px var(--neon-orange);
                color: #FFFFFF;
            }
        }
        
        /* DIGITAL SHAKE */
        @keyframes digitalShake {
            0%, 96%, 98%, 100% { 
                transform: translate(0);
            }
            97% { 
                transform: translate(1px, -1px);
            }
            99% { 
                transform: translate(-1px, 1px);
            }
        }
        
        .logo-subtitle {
            font-family: 'Share Tech Mono', monospace;
            font-size: 10px;
            color: var(--aqua-blue);
            text-transform: uppercase;
            letter-spacing: 3px;
            margin-top: 3px;
            text-shadow: var(--glow-aqua);
            opacity: 0.9;
            animation: subtitleScan 4s ease-in-out infinite;
        }
        
        @keyframes subtitleScan {
            0%, 100% { 
                opacity: 0.7;
                text-shadow: 0 0 5px var(--aqua-blue);
            }
            50% { 
                opacity: 1;
                text-shadow: 
                    0 0 10px var(--aqua-blue),
                    0 0 20px var(--aqua-blue);
            }
        }
        
        /* LOGO EFFECT ON HOVER */
        .logo-container:hover .logo-text {
            animation: hoverTech 0.5s ease-in-out infinite;
            color: var(--aqua-blue);
            text-shadow: var(--glow-aqua);
        }
        
        @keyframes hoverTech {
            0%, 100% { transform: translateY(0) translateX(0); }
            25% { transform: translateY(-2px) translateX(1px); }
            50% { transform: translateY(0) translateX(-1px); }
            75% { transform: translateY(2px) translateX(1px); }
        }
        
        /* NAVIGATION - TECH STYLE WITH ORIGINAL COLORS */
        nav ul {
            display: flex;
            list-style: none;
            gap: 25px;
        }
        
        nav a {
            color: var(--text-white);
            text-decoration: none;
            font-family: 'Rajdhani', sans-serif;
            font-weight: 600;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 2px;
            padding: 12px 0;
            position: relative;
            transition: var(--transition-fast);
        }
        
        nav a::before {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 2px;
            background-color: var(--neon-orange);
            transition: var(--transition-medium);
            box-shadow: var(--glow-orange);
        }
        
        nav a::after {
            content: '|';
            position: absolute;
            right: -15px;
            color: var(--aqua-blue);
            opacity: 0.5;
        }
        
        nav a:hover {
            color: var(--aqua-blue);
            text-shadow: var(--glow-aqua);
            animation: navDigital 0.3s steps(1, end);
        }
        
        @keyframes navDigital {
            0%, 100% { color: var(--aqua-blue); }
            50% { color: var(--electric-blue); }
        }
        
        nav a:hover::before {
            width: 100%;
        }
        
        /* MOBILE MENU BUTTON - TECH STYLE */
        .mobile-menu-btn {
            display: none;
            background-color: transparent;
            border: 2px solid var(--aqua-blue);
            color: var(--aqua-blue);
            width: 50px;
            height: 50px;
            border-radius: 0;
            cursor: pointer;
            font-size: 20px;
            transition: var(--transition-medium);
            position: relative;
            overflow: hidden;
            z-index: 10000;
        }
        
        .mobile-menu-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 212, 255, 0.1);
            transform: translateX(-100%);
            transition: var(--transition-medium);
        }
        
        .mobile-menu-btn:hover {
            border-color: var(--neon-orange);
            color: var(--neon-orange);
            box-shadow: var(--glow-orange);
            animation: btnDigital 0.3s steps(1, end);
        }
        
        @keyframes btnDigital {
            0%, 100% { transform: rotate(0); }
            50% { transform: rotate(5deg); }
        }
        
        .mobile-menu-btn:hover::before {
            transform: translateX(0);
        }
        
        /* === HERO SECTION - YOUR IMAGE === */
        .hero {
            min-height: 100vh;
            position: relative;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 120px 20px 100px;
            margin-top: 0;
        }
        
        /* HERO IMAGE BACKGROUND */
        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('https://www.dropbox.com/scl/fi/q7wp1xkc7215n80aejgrq/IMG_7314.jpeg?rlkey=41kcft32lvluzs7hzhxxo40zu&st=e5s65iff&dl=0');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            filter: brightness(0.4) contrast(1.2);
            z-index: -2;
        }
        
        /* COLOR OVERLAY */
        .hero::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(
                45deg,
                rgba(255, 74, 36, 0.15) 0%,
                rgba(70, 184, 255, 0.15) 100%
            );
            z-index: -1;
        }
        
        .hero-content {
            max-width: 800px;
            position: relative;
            z-index: 1;
            padding: 40px 30px;
            background-color: rgba(10, 14, 42, 0.7);
            backdrop-filter: blur(20px);
            border: 2px solid var(--aqua-blue);
            border-radius: 0;
            box-shadow: 
                var(--glow-blue),
                0 25px 70px rgba(0, 0, 0, 0.8);
            animation: contentScan 6s ease-in-out infinite;
            width: 100%;
            margin: 0 20px;
        }
        
        @keyframes contentScan {
            0%, 100% { 
                border-color: var(--aqua-blue);
                box-shadow: var(--glow-blue);
            }
            33% { 
                border-color: var(--neon-orange);
                box-shadow: var(--glow-orange);
            }
            66% { 
                border-color: var(--electric-blue);
                box-shadow: var(--glow-blue);
            }
        }
        
        .hero h1 {
            font-size: clamp(2.5rem, 8vw, 5.5rem);
            margin-bottom: 25px;
            letter-spacing: clamp(2px, 1vw, 5px);
            animation: 
                heroTechPulse 4s ease-in-out infinite,
                heroDigital 8s ease-in-out infinite;
            line-height: 1.1;
        }
        
        @keyframes heroTechPulse {
            0%, 100% { 
                text-shadow: var(--glow-orange);
            }
            50% { 
                text-shadow: 
                    0 0 30px var(--neon-orange),
                    0 0 60px var(--neon-orange),
                    0 0 90px var(--neon-orange);
                letter-spacing: 6px;
            }
        }
        
        @keyframes heroDigital {
            0%, 100% { 
                color: var(--neon-orange);
            }
            33% { 
                color: var(--aqua-blue);
            }
            66% { 
                color: var(--electric-blue);
            }
        }
        
        .hero p {
            font-family: 'Share Tech Mono', monospace;
            font-size: clamp(1rem, 3vw, 1.3rem);
            color: var(--text-light);
            margin-bottom: 40px;
            line-height: 1.8;
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.5);
            animation: textScan 8s ease-in-out infinite;
        }
        
        @keyframes textScan {
            0%, 100% { 
                opacity: 0.9;
                text-shadow: 0 0 5px var(--text-light);
            }
            50% { 
                opacity: 1;
                text-shadow: 
                    0 0 10px var(--text-light),
                    0 0 20px rgba(70, 184, 255, 0.5);
            }
        }
        
        /* BUTTONS - TECH STYLE WITH ORIGINAL COLORS */
        .btn {
            display: inline-block;
            background: linear-gradient(45deg, var(--neon-orange), var(--electric-blue));
            color: var(--text-white);
            padding: 18px 35px;
            border-radius: 0;
            text-decoration: none;
            font-family: 'Orbitron', sans-serif;
            font-weight: 700;
            font-size: clamp(14px, 2vw, 16px);
            text-transform: uppercase;
            letter-spacing: 3px;
            border: 2px solid var(--neon-orange);
            cursor: pointer;
            transition: var(--transition-medium);
            box-shadow: var(--glow-orange);
            position: relative;
            overflow: hidden;
        }
        
        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: var(--transition-medium);
        }
        
        .btn:hover {
            background: linear-gradient(45deg, var(--hot-red), var(--electric-blue));
            border-color: var(--hot-red);
            transform: translateY(-5px);
            box-shadow: 
                0 15px 40px rgba(232, 50, 26, 0.4),
                var(--glow-orange);
            letter-spacing: 4px;
            animation: btnTech 0.3s steps(1, end);
        }
        
        @keyframes btnTech {
            0%, 100% { 
                transform: translateY(-5px);
            }
            50% { 
                transform: translateY(-5px) translateX(2px);
            }
        }
        
        .btn:hover::before {
            left: 100%;
        }
        
        /* OUTLINE BUTTON - TECH STYLE */
        .btn-outline {
            display: inline-block;
            background-color: transparent;
            color: var(--aqua-blue);
            padding: 16px 30px;
            border-radius: 0;
            text-decoration: none;
            font-family: 'Orbitron', sans-serif;
            font-weight: 700;
            font-size: clamp(13px, 1.8vw, 15px);
            text-transform: uppercase;
            letter-spacing: 3px;
            border: 2px solid var(--aqua-blue);
            cursor: pointer;
            transition: var(--transition-medium);
            box-shadow: var(--glow-aqua);
            position: relative;
            overflow: hidden;
        }
        
        .btn-outline::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 212, 255, 0.1);
            transform: scaleX(0);
            transform-origin: left;
            transition: var(--transition-medium);
        }
        
        .btn-outline:hover {
            color: var(--text-white);
            border-color: var(--neon-orange);
            box-shadow: var(--glow-orange);
            transform: translateY(-3px);
            letter-spacing: 4px;
            animation: btnOutlineTech 0.3s steps(1, end);
        }
        
        @keyframes btnOutlineTech {
            0%, 100% { 
                transform: translateY(-3px);
            }
            50% { 
                transform: translateY(-3px) translateX(1px);
            }
        }
        
        .btn-outline:hover::before {
            transform: scaleX(1);
        }
        
        /* SECTION TITLES - TECH STYLE WITH ORIGINAL COLORS */
        .section-title {
            text-align: center;
            margin: 80px 0 50px;
            font-size: clamp(2.5rem, 6vw, 4rem);
            position: relative;
            padding-bottom: 25px;
            animation: 
                sectionTechPulse 5s ease-in-out infinite,
                sectionColorShift 10s ease-in-out infinite;
        }
        
        @keyframes sectionTechPulse {
            0%, 100% { 
                text-shadow: var(--glow-blue);
            }
            50% { 
                text-shadow: 
                    0 0 30px var(--electric-blue),
                    0 0 60px var(--electric-blue),
                    0 0 90px var(--electric-blue);
                letter-spacing: 5px;
            }
        }
        
        @keyframes sectionColorShift {
            0%, 100% { 
                color: var(--electric-blue);
            }
            33% { 
                color: var(--aqua-blue);
            }
            66% { 
                color: var(--neon-orange);
            }
        }
        
        .section-title::before,
        .section-title::after {
            content: '';
            position: absolute;
            bottom: 0;
            height: 3px;
            background-color: var(--aqua-blue);
            animation: 
                lineScan 2s ease-in-out infinite alternate,
                lineDigital 4s ease-in-out infinite;
        }
        
        .section-title::before {
            left: 50%;
            transform: translateX(-50%);
            width: 300px;
            box-shadow: var(--glow-aqua);
        }
        
        .section-title::after {
            left: 50%;
            transform: translateX(-50%);
            width: 150px;
            bottom: -10px;
            animation-delay: 0.5s;
            background-color: var(--neon-orange);
            box-shadow: var(--glow-orange);
        }
        
        @keyframes lineScan {
            0% { width: 300px; opacity: 0.7; }
            100% { width: 350px; opacity: 1; }
        }
        
        @keyframes lineDigital {
            0%, 100% { transform: translateX(-50%); }
            25% { transform: translateX(-49%); }
            50% { transform: translateX(-51%); }
            75% { transform: translateX(-50.5%); }
        }
        
        /* PRODUCT CARDS - TECH STYLE WITH ORIGINAL COLORS */
        .products-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 30px;
            max-width: 1400px;
            margin: 0 auto 80px;
            padding: 0 20px;
        }
        
        @media (min-width: 768px) {
            .products-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }
        
        @media (min-width: 1024px) {
            .products-grid {
                grid-template-columns: repeat(3, 1fr);
            }
        }
        
        .product-card {
            background-color: var(--navy-card);
            border: 2px solid var(--aqua-blue);
            border-radius: 0;
            overflow: hidden;
            transition: var(--transition-medium);
            position: relative;
            box-shadow: var(--shadow-strong);
            animation: cardTech 6s ease-in-out infinite;
        }
        
        @keyframes cardTech {
            0%, 100% { 
                border-color: var(--aqua-blue);
                box-shadow: var(--glow-aqua);
            }
            33% { 
                border-color: var(--neon-orange);
                box-shadow: var(--glow-orange);
            }
            66% { 
                border-color: var(--electric-blue);
                box-shadow: var(--glow-blue);
            }
        }
        
        .product-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            border: 2px solid var(--neon-orange);
            opacity: 0;
            transition: var(--transition-medium);
            z-index: 1;
            pointer-events: none;
        }
        
        .product-card:hover {
            transform: translateY(-15px) scale(1.03);
            border-color: var(--neon-orange);
            box-shadow: 
                var(--shadow-hover),
                var(--glow-orange);
            animation: cardHoverTech 0.5s steps(1, end);
        }
        
        @keyframes cardHoverTech {
            0%, 100% { transform: translateY(-15px) scale(1.03); }
            50% { transform: translateY(-15px) scale(1.03) translateX(2px); }
        }
        
        .product-card:hover::before {
            opacity: 1;
            animation: borderDigital 1.5s infinite;
        }
        
        @keyframes borderDigital {
            0%, 100% { opacity: 0.3; }
            50% { opacity: 0.8; }
        }
        
        .product-img {
            width: 100%;
            height: 280px;
            object-fit: cover;
            transition: var(--transition-medium);
            filter: grayscale(0.3) contrast(1.2);
        }
        
        /* FALLBACK FOR BROKEN IMAGES */
        .product-img:not([src]),
        .product-img[src=""],
        .product-img[src*="undefined"],
        .product-img[src*="null"] {
            background: linear-gradient(45deg, #111535, #13183C);
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .product-img:not([src])::after,
        .product-img[src=""]::after,
        .product-img[src*="undefined"]::after,
        .product-img[src*="null"]::after {
            content: ">_";
            font-family: 'Share Tech Mono', monospace;
            font-size: 3rem;
            color: var(--aqua-blue);
            animation: loadingDots 1.5s infinite;
        }
        
        @keyframes loadingDots {
            0% { content: ">_"; }
            33% { content: ">_."; }
            66% { content: ">_.."; }
            100% { content: ">_..."; }
        }
        
        .product-card:hover .product-img {
            filter: grayscale(0) contrast(1.4);
            transform: scale(1.05);
        }
        
        .product-info {
            padding: 25px;
            position: relative;
            background-color: var(--navy-card);
            border-top: 2px solid var(--navy-border);
        }
        
        .product-name {
            font-family: 'Audiowide', 'Orbitron', sans-serif;
            font-size: 1.5rem;
            margin-bottom: 15px;
            color: var(--aqua-blue);
            text-shadow: var(--glow-aqua);
            transition: var(--transition-medium);
        }
        
        .product-card:hover .product-name {
            animation: productNameTech 0.3s steps(1, end);
            color: var(--neon-orange);
            text-shadow: var(--glow-orange);
        }
        
        @keyframes productNameTech {
            0%, 100% { color: var(--neon-orange); }
            50% { color: var(--electric-blue); }
        }
        
        .product-desc {
            color: var(--text-light);
            margin-bottom: 25px;
            line-height: 1.7;
            font-size: 1rem;
            font-family: 'Share Tech Mono', monospace;
        }
        
        /* ABOUT SECTION - TECH STYLE WITH ORIGINAL COLORS */
        .about {
            padding: 80px 20px;
            background-color: var(--dark-navy);
            border-top: 2px solid var(--navy-border);
            border-bottom: 2px solid var(--navy-border);
            position: relative;
            overflow: hidden;
        }
        
        /* DIGITAL SCANNING LINE */
        .about::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(90deg, 
                transparent, 
                var(--neon-orange), 
                var(--aqua-blue), 
                var(--electric-blue),
                transparent);
            animation: 
                digitalScan 3s linear infinite,
                scanPulse 2s ease-in-out infinite;
        }
        
        @keyframes digitalScan {
            0% { transform: translateX(-100%); }
            100% { transform: translateX(100%); }
        }
        
        @keyframes scanPulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }
        
        .about-content {
            display: flex;
            flex-direction: column;
            gap: 40px;
            max-width: 1400px;
            margin: 0 auto;
            align-items: center;
        }
        
        @media (min-width: 1024px) {
            .about-content {
                flex-direction: row;
            }
        }
        
        .about-img {
            flex: 1;
            min-height: 350px;
            background-image: url('https://i.postimg.cc/LX8dfbLS/IMG_7106.jpg');
            background-size: cover;
            background-position: center;
            border: 2px solid var(--aqua-blue);
            position: relative;
            overflow: hidden;
            box-shadow: var(--glow-aqua);
            animation: imageTech 8s ease-in-out infinite;
        }
        
        @keyframes imageTech {
            0%, 100% { 
                border-color: var(--aqua-blue);
                box-shadow: var(--glow-aqua);
            }
            33% { 
                border-color: var(--neon-orange);
                box-shadow: var(--glow-orange);
            }
            66% { 
                border-color: var(--electric-blue);
                box-shadow: var(--glow-blue);
            }
        }
        
        .about-img::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(45deg, 
                rgba(255, 74, 36, 0.2) 0%, 
                rgba(0, 212, 255, 0.2) 50%, 
                rgba(70, 184, 255, 0.2) 100%);
        }
        
        .about-text {
            flex: 1;
            padding: 20px;
        }
        
        .about-text h2 {
            font-size: clamp(2rem, 5vw, 3.2rem);
            margin-bottom: 25px;
            color: var(--neon-orange);
            animation: 
                aboutTitleTech 7s ease-in-out infinite,
                aboutTitlePulse 5s ease-in-out infinite;
        }
        
        @keyframes aboutTitleTech {
            0%, 100% { 
                color: var(--neon-orange);
            }
            33% { 
                color: var(--aqua-blue);
            }
            66% { 
                color: var(--electric-blue);
            }
        }
        
        @keyframes aboutTitlePulse {
            0%, 100% { 
                text-shadow: var(--glow-orange);
            }
            50% { 
                text-shadow: 
                    0 0 25px var(--neon-orange),
                    0 0 50px var(--neon-orange);
            }
        }
        
        .about-text p {
            color: var(--text-light);
            margin-bottom: 20px;
            line-height: 1.8;
            font-size: 1.05rem;
            font-family: 'Share Tech Mono', monospace;
            padding: 20px;
            background-color: rgba(26, 31, 68, 0.5);
            border-left: 3px solid var(--aqua-blue);
            transition: var(--transition-medium);
        }
        
        .about-text p:hover {
            animation: textTech 0.3s steps(1, end);
            border-left-color: var(--neon-orange);
        }
        
        @keyframes textTech {
            0%, 100% { transform: translateX(0); }
            50% { transform: translateX(2px); }
        }
        
        /* VALUES CARDS - TECH STYLE WITH ORIGINAL COLORS */
        .values {
            padding: 80px 20px;
            background-color: var(--deep-navy);
            position: relative;
        }
        
        .values-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 25px;
            max-width: 1400px;
            margin: 0 auto;
        }
        
        @media (min-width: 768px) {
            .values-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }
        
        @media (min-width: 1024px) {
            .values-grid {
                grid-template-columns: repeat(4, 1fr);
            }
        }
        
        .value-card {
            background-color: var(--navy-card);
            border: 2px solid var(--navy-border);
            padding: 35px 25px;
            text-align: center;
            position: relative;
            overflow: hidden;
            transition: var(--transition-medium);
            box-shadow: var(--shadow-strong);
            animation: valueCardTech 6s ease-in-out infinite;
        }
        
        @keyframes valueCardTech {
            0%, 100% { 
                border-color: var(--navy-border);
            }
            33% { 
                border-color: var(--aqua-blue);
            }
            66% { 
                border-color: var(--neon-orange);
            }
        }
        
        .value-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background-color: var(--aqua-blue);
            transform: scaleX(0);
            transition: var(--transition-medium);
            box-shadow: var(--glow-aqua);
        }
        
        .value-card:hover {
            transform: translateY(-12px) scale(1.03);
            border-color: var(--neon-orange);
            box-shadow: 
                var(--shadow-hover),
                var(--glow-orange);
            animation: valueCardHoverTech 0.4s steps(1, end);
        }
        
        @keyframes valueCardHoverTech {
            0%, 100% { transform: translateY(-12px) scale(1.03); }
            50% { transform: translateY(-12px) scale(1.03) rotate(1deg); }
        }
        
        .value-card:hover::before {
            transform: scaleX(1);
        }
        
        .value-icon {
            font-size: 3rem;
            margin-bottom: 20px;
            color: var(--neon-orange);
            text-shadow: var(--glow-orange);
            transition: var(--transition-medium);
            animation: iconTech 5s ease-in-out infinite;
        }
        
        @keyframes iconTech {
            0%, 100% { 
                color: var(--neon-orange);
                text-shadow: var(--glow-orange);
            }
            33% { 
                color: var(--aqua-blue);
                text-shadow: var(--glow-aqua);
            }
            66% { 
                color: var(--electric-blue);
                text-shadow: var(--glow-blue);
            }
        }
        
        .value-card:hover .value-icon {
            transform: scale(1.3) rotate(15deg);
            color: var(--aqua-blue);
            text-shadow: var(--glow-aqua);
            animation: iconHoverTech 0.3s steps(1, end);
        }
        
        @keyframes iconHoverTech {
            0%, 100% { color: var(--aqua-blue); }
            50% { color: var(--neon-orange); }
        }
        
        .value-card h3 {
            font-family: 'Audiowide', 'Orbitron', sans-serif;
            font-size: 1.3rem;
            margin-bottom: 15px;
            color: var(--aqua-blue);
            text-transform: uppercase;
            letter-spacing: 2px;
        }
        
        .value-card:hover h3 {
            animation: valueTitleTech 0.3s steps(1, end);
            color: var(--neon-orange);
        }
        
        @keyframes valueTitleTech {
            0%, 100% { color: var(--neon-orange); }
            50% { color: var(--electric-blue); }
        }
        
        .value-card p {
            color: var(--text-light);
            font-size: 0.95rem;
            line-height: 1.6;
            font-family: 'Share Tech Mono', monospace;
        }
        
        /* GALLERY SECTION - TECH STYLE WITH ORIGINAL COLORS */
        .gallery-section {
            padding: 80px 20px;
            background-color: var(--dark-navy);
            border-top: 2px solid var(--navy-border);
            position: relative;
            overflow: hidden;
        }
        
        .slide-gallery {
            max-width: 1400px;
            margin: 50px auto 0;
            border: 2px solid var(--electric-blue);
            border-radius: 0;
            overflow: hidden;
            box-shadow: var(--glow-blue);
            position: relative;
            animation: galleryTech 9s ease-in-out infinite;
        }
        
        @keyframes galleryTech {
            0%, 100% { 
                border-color: var(--electric-blue);
                box-shadow: var(--glow-blue);
            }
            33% { 
                border-color: var(--aqua-blue);
                box-shadow: var(--glow-aqua);
            }
            66% { 
                border-color: var(--neon-orange);
                box-shadow: var(--glow-orange);
            }
        }
        
        .slides-container {
            display: flex;
            transition: transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
            height: 400px;
        }
        
        @media (min-width: 768px) {
            .slides-container {
                height: 500px;
            }
        }
        
        @media (min-width: 1024px) {
            .slides-container {
                height: 550px;
            }
        }
        
        .slide {
            min-width: 100%;
            position: relative;
        }
        
        .slide-img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: var(--transition-medium);
            filter: contrast(1.2) brightness(0.8);
        }
        
        .slide:hover .slide-img {
            filter: contrast(1.4) brightness(1);
            transform: scale(1.05);
        }
        
        .slide-caption {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            background-color: rgba(10, 14, 42, 0.9);
            color: var(--text-white);
            padding: 20px;
            font-family: 'Audiowide', 'Orbitron', sans-serif;
            font-size: clamp(1rem, 3vw, 1.4rem);
            text-transform: uppercase;
            letter-spacing: 3px;
            text-align: center;
            backdrop-filter: blur(10px);
            border-top: 2px solid var(--neon-orange);
            text-shadow: var(--glow-orange);
            animation: captionTech 7s ease-in-out infinite;
        }
        
        @keyframes captionTech {
            0%, 100% { 
                border-color: var(--neon-orange);
                text-shadow: var(--glow-orange);
            }
            33% { 
                border-color: var(--aqua-blue);
                text-shadow: var(--glow-aqua);
            }
            66% { 
                border-color: var(--electric-blue);
                text-shadow: var(--glow-blue);
            }
        }
        
        .gallery-controls {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 20px;
            margin-top: 30px;
            flex-wrap: wrap;
        }
        
        .prev-btn, .next-btn {
            width: 55px;
            height: 55px;
            border: 2px solid var(--aqua-blue);
            background-color: transparent;
            color: var(--aqua-blue);
            border-radius: 0;
            cursor: pointer;
            font-size: 1.5rem;
            transition: var(--transition-medium);
            position: relative;
            overflow: hidden;
            animation: controlTech 5s ease-in-out infinite;
            flex-shrink: 0;
        }
        
        @keyframes controlTech {
            0%, 100% { 
                border-color: var(--aqua-blue);
                color: var(--aqua-blue);
                box-shadow: var(--glow-aqua);
            }
            33% { 
                border-color: var(--neon-orange);
                color: var(--neon-orange);
                box-shadow: var(--glow-orange);
            }
            66% { 
                border-color: var(--electric-blue);
                color: var(--electric-blue);
                box-shadow: var(--glow-blue);
            }
        }
        
        .prev-btn::before, .next-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 212, 255, 0.1);
            transform: translateX(-100%);
            transition: var(--transition-medium);
        }
        
        .prev-btn:hover, .next-btn:hover {
            border-color: var(--neon-orange);
            color: var(--neon-orange);
            box-shadow: var(--glow-orange);
            transform: rotate(90deg) scale(1.1);
            animation: controlHoverTech 0.4s steps(1, end);
        }
        
        @keyframes controlHoverTech {
            0%, 100% { transform: rotate(90deg) scale(1.1); }
            50% { transform: rotate(92deg) scale(1.1); }
        }
        
        .prev-btn:hover::before, .next-btn:hover::before {
            transform: translateX(0);
        }
        
        .gallery-dots {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
            justify-content: center;
        }
        
        .dot {
            width: 16px;
            height: 16px;
            border: 2px solid var(--aqua-blue);
            border-radius: 0;
            cursor: pointer;
            transition: var(--transition-medium);
            position: relative;
            background-color: transparent;
            transform: rotate(45deg);
            animation: dotTech 4s ease-in-out infinite;
            flex-shrink: 0;
        }
        
        @keyframes dotTech {
            0%, 100% { 
                border-color: var(--aqua-blue);
                box-shadow: var(--glow-aqua);
            }
            33% { 
                border-color: var(--neon-orange);
                box-shadow: var(--glow-orange);
            }
            66% { 
                border-color: var(--electric-blue);
                box-shadow: var(--glow-blue);
            }
        }
        
        .dot::before {
            content: '';
            position: absolute;
            top: 2px;
            left: 2px;
            right: 2px;
            bottom: 2px;
            background-color: var(--neon-orange);
            opacity: 0;
            transition: var(--transition-medium);
        }
        
        .dot.active {
            border-color: var(--neon-orange);
            box-shadow: var(--glow-orange);
            animation: activeDotTech 2s steps(1, end) infinite;
        }
        
        @keyframes activeDotTech {
            0%, 100% { 
                border-color: var(--neon-orange);
                box-shadow: var(--glow-orange);
            }
            50% { 
                border-color: var(--aqua-blue);
                box-shadow: var(--glow-aqua);
            }
        }
        
        .dot.active::before {
            opacity: 1;
            animation: dotInnerTech 1s steps(1, end) infinite;
        }
        
        @keyframes dotInnerTech {
            0%, 100% { background-color: var(--neon-orange); }
            50% { background-color: var(--aqua-blue); }
        }
        
        .dot:hover {
            border-color: var(--neon-orange);
            transform: rotate(135deg) scale(1.3);
            animation: dotHoverTech 0.3s steps(1, end);
        }
        
        @keyframes dotHoverTech {
            0%, 100% { transform: rotate(135deg) scale(1.3); }
            50% { transform: rotate(137deg) scale(1.3); }
        }
        
        /* FOOTER - TECH STYLE WITH ORIGINAL COLORS */
        footer {
            background-color: var(--deep-navy);
            border-top: 3px solid var(--neon-orange);
            padding: 60px 20px 25px;
            position: relative;
            overflow: hidden;
        }
        
        footer::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(90deg, 
                transparent, 
                var(--aqua-blue), 
                var(--neon-orange), 
                var(--electric-blue),
                transparent);
            animation: 
                footerScan 6s linear infinite,
                footerTechPulse 3s ease-in-out infinite;
        }
        
        @keyframes footerScan {
            0% { transform: translateX(-100%); }
            100% { transform: translateX(100%); }
        }
        
        @keyframes footerTechPulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }
        
        .footer-content {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr;
            gap: 40px;
            position: relative;
            z-index: 1;
        }
        
        @media (min-width: 768px) {
            .footer-content {
                grid-template-columns: repeat(3, 1fr);
            }
        }
        
        .footer-logo {
            font-family: 'Wallpoet', 'Audiowide', sans-serif;
            font-size: clamp(2rem, 5vw, 2.5rem);
            font-weight: 900;
            color: var(--neon-orange);
            margin-bottom: 20px;
            text-shadow: var(--glow-orange);
            animation: 
                footerLogoTech 7s ease-in-out infinite,
                footerLogoPulse 5s ease-in-out infinite;
        }
        
        @keyframes footerLogoTech {
            0%, 100% { 
                color: var(--neon-orange);
                text-shadow: var(--glow-orange);
            }
            33% { 
                color: var(--aqua-blue);
                text-shadow: var(--glow-aqua);
            }
            66% { 
                color: var(--electric-blue);
                text-shadow: var(--glow-blue);
            }
        }
        
        @keyframes footerLogoPulse {
            0%, 100% { 
                opacity: 1;
            }
            50% { 
                opacity: 0.8;
                letter-spacing: 3px;
            }
        }
        
        .footer-section h3 {
            font-family: 'Audiowide', 'Orbitron', sans-serif;
            font-size: 1.4rem;
            margin-bottom: 25px;
            color: var(--aqua-blue);
            text-transform: uppercase;
            letter-spacing: 2px;
            padding-bottom: 12px;
            border-bottom: 2px solid var(--navy-border);
            animation: footerTitleTech 6s ease-in-out infinite;
        }
        
        @keyframes footerTitleTech {
            0%, 100% { 
                color: var(--aqua-blue);
            }
            33% { 
                color: var(--neon-orange);
            }
            66% { 
                color: var(--electric-blue);
            }
        }
        
        .social-icons {
            display: flex;
            gap: 12px;
            margin-top: 25px;
            flex-wrap: wrap;
        }
        
        .social-icons a {
            width: 50px;
            height: 50px;
            border: 2px solid var(--aqua-blue);
            border-radius: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--aqua-blue);
            font-size: 1.3rem;
            transition: var(--transition-medium);
            position: relative;
            overflow: hidden;
            animation: socialTech 5s ease-in-out infinite;
            flex-shrink: 0;
        }
        
        @keyframes socialTech {
            0%, 100% { 
                border-color: var(--aqua-blue);
                color: var(--aqua-blue);
                box-shadow: var(--glow-aqua);
            }
            33% { 
                border-color: var(--neon-orange);
                color: var(--neon-orange);
                box-shadow: var(--glow-orange);
            }
            66% { 
                border-color: var(--electric-blue);
                color: var(--electric-blue);
                box-shadow: var(--glow-blue);
            }
        }
        
        .social-icons a::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 212, 255, 0.1);
            transform: translateX(-100%);
            transition: var(--transition-medium);
        }
        
        .social-icons a:hover {
            border-color: var(--neon-orange);
            color: var(--neon-orange);
            transform: rotate(45deg) scale(1.2);
            box-shadow: var(--glow-orange);
            animation: socialHoverTech 0.4s steps(1, end);
        }
        
        @keyframes socialHoverTech {
            0%, 100% { transform: rotate(45deg) scale(1.2); }
            50% { transform: rotate(47deg) scale(1.2); }
        }
        
        .social-icons a:hover::before {
            transform: translateX(0);
        }
        
        .footer-links {
            list-style: none;
        }
        
        .footer-links li {
            margin-bottom: 12px;
        }
        
        .footer-links a {
            color: var(--text-light);
            text-decoration: none;
            font-family: 'Rajdhani', sans-serif;
            font-size: 1rem;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            transition: var(--transition-fast);
            display: inline-block;
            padding: 5px 0;
            position: relative;
        }
        
        .footer-links a::before {
            content: '>';
            position: absolute;
            left: -20px;
            color: var(--aqua-blue);
            opacity: 0;
            transition: var(--transition-fast);
        }
        
        .footer-links a:hover {
            color: var(--aqua-blue);
            padding-left: 15px;
            text-shadow: var(--glow-aqua);
            animation: linkTech 0.3s steps(1, end);
        }
        
        @keyframes linkTech {
            0%, 100% { 
                color: var(--aqua-blue);
                transform: translateX(0);
            }
            50% { 
                color: var(--neon-orange);
                transform: translateX(2px);
            }
        }
        
        .footer-links a:hover::before {
            opacity: 1;
            left: -10px;
            animation: arrowTech 0.3s steps(1, end);
        }
        
        @keyframes arrowTech {
            0%, 100% { color: var(--aqua-blue); }
            50% { color: var(--neon-orange); }
        }
        
        .contact-info div {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-bottom: 15px;
            color: var(--text-light);
            font-family: 'Share Tech Mono', monospace;
        }
        
        .contact-info i {
            color: var(--neon-orange);
            width: 25px;
            font-size: 1.2rem;
            text-shadow: var(--glow-orange);
            animation: contactIconTech 6s ease-in-out infinite;
        }
        
        @keyframes contactIconTech {
            0%, 100% { 
                color: var(--neon-orange);
                text-shadow: var(--glow-orange);
            }
            33% { 
                color: var(--aqua-blue);
                text-shadow: var(--glow-aqua);
            }
            66% { 
                color: var(--electric-blue);
                text-shadow: var(--glow-blue);
            }
        }
        
        .copyright {
            text-align: center;
            margin-top: 50px;
            padding-top: 25px;
            border-top: 2px solid var(--navy-border);
            color: var(--text-muted);
            font-size: 0.9rem;
            letter-spacing: 2px;
            font-family: 'Share Tech Mono', monospace;
            animation: copyrightTech 8s ease-in-out infinite;
        }
        
        @keyframes copyrightTech {
            0%, 100% { 
                opacity: 0.8;
            }
            50% { 
                opacity: 1;
                color: var(--text-light);
            }
        }
        
        .highlight {
            color: var(--neon-orange);
            font-weight: 700;
            text-shadow: var(--glow-orange);
            font-family: 'Wallpoet', 'Audiowide', sans-serif;
            animation: 
                highlightTech 5s ease-in-out infinite,
                highlightPulse 3s ease-in-out infinite;
        }
        
        @keyframes highlightTech {
            0%, 100% { 
                color: var(--neon-orange);
                text-shadow: var(--glow-orange);
            }
            33% { 
                color: var(--aqua-blue);
                text-shadow: var(--glow-aqua);
            }
            66% { 
                color: var(--electric-blue);
                text-shadow: var(--glow-blue);
            }
        }
        
        @keyframes highlightPulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }
        
        /* MOBILE RESPONSIVE */
        @media (max-width: 1023px) {
            nav ul {
                display: none;
                position: fixed;
                top: 0;
                left: 0;
                width: 100%;
                height: 100vh;
                background-color: rgba(10, 14, 42, 0.98);
                flex-direction: column;
                justify-content: center;
                align-items: center;
                backdrop-filter: blur(20px);
                z-index: 9998;
                padding: 80px 20px;
                border: 2px solid var(--neon-orange);
                gap: 30px;
            }
            
            nav ul.active {
                display: flex;
            }
            
            nav a::after {
                display: none;
            }
            
            nav a {
                font-size: 18px;
                padding: 15px 0;
            }
            
            .mobile-menu-btn {
                display: flex;
                align-items: center;
                justify-content: center;
            }
            
            .hero-content {
                padding: 30px 20px;
            }
            
            .logo-text {
                font-size: 24px;
            }
            
            .logo-subtitle {
                font-size: 8px;
            }
            
            .section-title::before {
                width: 200px;
            }
            
            .section-title::after {
                width: 100px;
            }
            
            .about-content {
                gap: 30px;
            }
            
            .about-img {
                min-height: 300px;
            }
        }
        
        @media (max-width: 767px) {
            .hero h1 {
                font-size: 2.5rem;
            }
            
            .hero p {
                font-size: 1.1rem;
            }
            
            .btn {
                padding: 16px 30px;
                font-size: 14px;
            }
            
            .btn-outline {
                padding: 14px 25px;
                font-size: 13px;
            }
            
            .section-title {
                font-size: 2.2rem;
                margin: 60px 0 40px;
            }
            
            .products-grid {
                gap: 25px;
            }
            
            .product-card, .value-card {
                padding: 20px;
            }
            
            .product-img {
                height: 250px;
            }
            
            .slide-caption {
                padding: 15px;
                font-size: 1rem;
            }
            
            .prev-btn, .next-btn {
                width: 50px;
                height: 50px;
                font-size: 1.3rem;
            }
            
            .gallery-controls {
                gap: 15px;
            }
            
            .footer-content {
                gap: 30px;
            }
        }
        
        @media (max-width: 480px) {
            .hero {
                padding: 100px 20px 80px;
            }
            
            .hero h1 {
                font-size: 2rem;
            }
            
            .hero p {
                font-size: 1rem;
            }
            
            .section-title {
                font-size: 1.8rem;
            }
            
            .product-name {
                font-size: 1.3rem;
            }
            
            .value-card h3 {
                font-size: 1.2rem;
            }
            
            .value-icon {
                font-size: 2.5rem;
            }
            
            .footer-logo {
                font-size: 1.8rem;
            }
            
            .footer-section h3 {
                font-size: 1.2rem;
            }
            
            .social-icons a {
                width: 45px;
                height: 45px;
                font-size: 1.2rem;
            }
        }
        
        /* ACCESSIBILITY */
        .sr-only {
            position: absolute;
            width: 1px;
            height: 1px;
            padding: 0;
            margin: -1px;
            overflow: hidden;
            clip: rect(0, 0, 0, 0);
            white-space: nowrap;
            border: 0;
        }
        
        /* LOADING ANIMATION - TECH STYLE WITH ORIGINAL COLORS */
        .loading {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: var(--deep-navy);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            z-index: 99999;
            transition: opacity 0.5s ease;
        }
        
        .cyber-loader {
            width: 60px;
            height: 60px;
            position: relative;
        }
        
        @media (min-width: 768px) {
            .cyber-loader {
                width: 80px;
                height: 80px;
            }
        }
        
        .cyber-loader div {
            position: absolute;
            width: 100%;
            height: 100%;
            border: 3px solid transparent;
            border-top-color: var(--neon-orange);
            border-radius: 50%;
            animation: 
                cyberSpin 2s linear infinite,
                loaderTechPulse 3s ease-in-out infinite;
        }
        
        .cyber-loader div:nth-child(2) {
            border-top-color: var(--aqua-blue);
            animation-delay: -1s;
        }
        
        @keyframes cyberSpin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        @keyframes loaderTechPulse {
            0%, 100% { 
                border-top-color: var(--neon-orange);
                box-shadow: var(--glow-orange);
            }
            50% { 
                border-top-color: var(--aqua-blue);
                box-shadow: var(--glow-aqua);
            }
        }
        
        .loading-text {
            margin-top: 20px;
            color: var(--aqua-blue);
            font-family: 'Share Tech Mono', monospace;
            text-transform: uppercase;
            letter-spacing: 3px;
            font-size: 1rem;
            animation: 
                loadingTech 2s steps(1, end) infinite,
                loadingColorTech 4s ease-in-out infinite;
            text-align: center;
            padding: 0 20px;
        }
        
        @media (min-width: 768px) {
            .loading-text {
                font-size: 1.2rem;
                letter-spacing: 4px;
            }
        }
        
        @keyframes loadingTech {
            0% { content: "BUSY BODY SOAPS"; }
            25% { content: "BUSY B0DY SOAPS"; }
            50% { content: "BUSY B0DY S0APS"; }
            75% { content: "BUSY B0DY SOAPS"; }
            100% { content: "BUSY BODY SOAPS"; }
        }
        
        @keyframes loadingColorTech {
            0%, 100% { 
                color: var(--aqua-blue);
                text-shadow: var(--glow-aqua);
            }
            33% { 
                color: var(--neon-orange);
                text-shadow: var(--glow-orange);
            }
            66% { 
                color: var(--electric-blue);
                text-shadow: var(--glow-blue);
            }
        }
    </style>
    
    <!-- LAZY LOADING + PERFORMANCE -->
    <link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;600;700;800;900&family=Rajdhani:wght@300;400;500;600;700&family=Share+Tech+Mono&family=Exo+2:wght@300;400;500;600;700;800;900&family=Audiowide&family=Geo:ital@0;1&family=Monoton&family=Wallpoet&display=swap">
    <link rel="preload" as="style" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="preload" as="image" href="https://www.dropbox.com/scl/fi/q7wp1xkc7215n80aejgrq/IMG_7314.jpeg?rlkey=41kcft32lvluzs7hzhxxo40zu&st=e5s65iff&dl=0">
</head>
<body>
    <!-- LOADING SCREEN -->
    <div class="loading" id="loading">
        <div class="cyber-loader">
            <div></div>
            <div></div>
        </div>
        <div class="loading-text">BUSY BODY SOAPS</div>
    </div>
    
    <!-- HEADER -->
    <header>
        <div class="header-container">
            <div class="logo-container">
                <div class="logo-text">BUSY BODY SOAPS</div>
                <div class="logo-subtitle">HANDCRAFTED WITH CARE</div>
            </div>
            
            <button class="mobile-menu-btn" id="mobileMenuBtn" aria-label="Toggle navigation menu">
                <i class="fas fa-bars"></i>
            </button>
            
            <nav>
                <ul id="navMenu">
                    <li><a href="#home" aria-label="Home section">Home</a></li>
                    <li><a href="#products" aria-label="Products section">Products</a></li>
                    <li><a href="#about" aria-label="About section">About</a></li>
                    <li><a href="#values" aria-label="Values section">Values</a></li>
                    <li><a href="#gallery" aria-label="Gallery section">Gallery</a></li>
                </ul>
            </nav>
        </div>
    </header>
    
    <!-- HERO -->
    <section class="hero" id="home">
        <div class="hero-content">
            <h1>Handcrafted Soap Art</h1>
            <p>Natural, clean, and thoughtfully crafted soaps made with only the finest ingredients. Each bar is a unique creation designed to elevate your daily routine.</p>
            <a href="#products" class="btn" aria-label="Shop our collection">Shop Collection</a>
        </div>
    </section>
    
    <!-- PRODUCTS -->
    <section class="products" id="products">
        <div class="container">
            <h2 class="section-title">Our Collection</h2>
            <div class="products-grid">
                <div class="product-card">
                    <img src="https://i.postimg.cc/mDgxM5Cs/IMG_7110.jpg" 
                         alt="Aloe Vera and Turmeric Soap" 
                         class="product-img"
                         loading="lazy">
                    <div class="product-info">
                        <h3 class="product-name">Aloe Vera & Turmeric</h3>
                        <p class="product-desc">Soothing aloe vera combined with anti-inflammatory turmeric for calm, radiant skin. Perfect for sensitive or irritated skin types.</p>
                        <a href="#" class="btn-outline" aria-label="View Aloe Vera & Turmeric soap details">View Details</a>
                    </div>
                </div>
                
                <div class="product-card">
                    <img src="https://i.postimg.cc/B6nWDVDm/IMG_7116.jpg" 
                         alt="Lavender and Honey Soap" 
                         class="product-img"
                         loading="lazy">
                    <div class="product-info">
                        <h3 class="product-name">Lavender & Honey</h3>
                        <p class="product-desc">Calming lavender essential oil paired with moisturizing raw honey. A relaxing blend perfect for evening baths and dry skin.</p>
                        <a href="#" class="btn-outline" aria-label="View Lavender & Honey soap details">View Details</a>
                    </div>
                </div>
                
                <div class="product-card">
                    <img src="https://i.postimg.cc/pTLN868S/IMG_7108.jpg" 
                         alt="Bamboo Charcoal and Turmeric Soap" 
                         class="product-img"
                         loading="lazy">
                    <div class="product-info">
                        <h3 class="product-name">Bamboo Charcoal & Turmeric</h3>
                        <p class="product-desc">Detoxifying bamboo charcoal combined with brightening turmeric. Deep cleans pores while reducing inflammation and blemishes.</p>
                        <a href="#" class="btn-outline" aria-label="View Bamboo Charcoal & Turmeric soap details">View Details</a>
                    </div>
                </div>
            </div>
        </div>
    </section>
    
    <!-- ABOUT -->
    <section class="about" id="about">
        <div class="container">
            <h2 class="section-title">About Busy Body Soaps</h2>
            <div class="about-content">
                <div class="about-img"></div>
                <div class="about-text">
                    <h2>Crafting with Purpose</h2>
                    <p>Busy Body Soaps was born from a passion for creating beautiful, functional products that honor both artistry and simplicity. Each soap is handcrafted with intention, using only natural ingredients that nourish the skin.</p>
                    <p>Our process combines traditional soapmaking techniques with modern design sensibilities. We believe that the products we use daily should be both effective and beautiful, creating moments of mindfulness in everyday routines.</p>
                    <p>From selecting sustainable ingredients to designing each bar's aesthetic, every step is done with care and attention to detail.</p>
                    <a href="#gallery" class="btn" aria-label="View our gallery">View Our Gallery</a>
                </div>
            </div>
        </div>
    </section>
    
    <!-- VALUES -->
    <section class="values" id="values">
        <div class="container">
            <h2 class="section-title">Our Values</h2>
            <div class="values-grid">
                <div class="value-card">
                    <div class="value-icon">
                        <i class="fas fa-leaf"></i>
                    </div>
                    <h3>Natural Ingredients</h3>
                    <p>We use only plant-based oils, butters, and botanicals in our formulations.</p>
                </div>
                
                <div class="value-card">
                    <div class="value-icon">
                        <i class="fas fa-hand-rock"></i>
                    </div>
                    <h3>Handcrafted</h3>
                    <p>Every bar is made by hand in small batches for quality and attention to detail.</p>
                </div>
                
                <div class="value-card">
                    <div class="value-icon">
                        <i class="fas fa-palette"></i>
                    </div>
                    <h3>Artistic Design</h3>
                    <p>We believe everyday products should be beautiful as well as functional.</p>
                </div>
                
                <div class="value-card">
                    <div class="value-icon">
                        <i class="fas fa-recycle"></i>
                    </div>
                    <h3>Sustainable</h3>
                    <p>Eco-friendly packaging and responsible sourcing are central to our practice.</p>
                </div>
            </div>
        </div>
    </section>
    
    <!-- GALLERY -->
    <section class="gallery-section" id="gallery">
        <div class="container">
            <h2 class="section-title">Our Gallery</h2>
            
            <div class="slide-gallery">
                <div class="slides-container" id="slidesContainer">
                    <div class="slide">
                        <img src="https://i.postimg.cc/mDgxM5Cs/IMG_7110.jpg" 
                             alt="Aloe Vera and Turmeric Soap Detail" 
                             class="slide-img"
                             loading="lazy">
                        <div class="slide-caption">Aloe Vera & Turmeric Soap</div>
                    </div>
                    <div class="slide">
                        <img src="https://i.postimg.cc/B6nWDVDm/IMG_7116.jpg" 
                             alt="Lavender and Honey Soap Detail" 
                             class="slide-img"
                             loading="lazy">
                        <div class="slide-caption">Lavender & Honey Soap</div>
                    </div>
                    <div class="slide">
                        <img src="https://i.postimg.cc/pTLN868S/IMG_7108.jpg" 
                             alt="Bamboo Charcoal and Turmeric Soap Detail" 
                             class="slide-img"
                             loading="lazy">
                        <div class="slide-caption">Bamboo Charcoal & Turmeric Soap</div>
                    </div>
                    <div class="slide">
                        <img src="https://i.postimg.cc/DZwVsMsY/IMG_7109.jpg" 
                             alt="Soap Making Process" 
                             class="slide-img"
                             loading="lazy">
                        <div class="slide-caption">The Soap Making Process</div>
                    </div>
                    <div class="slide">
                        <img src="https://i.postimg.cc/zDLVLPGC/IMG_7130.jpg" 
                             alt="Soap Collection Display" 
                             class="slide-img"
                             loading="lazy">
                        <div class="slide-caption">Our Soap Collection</div>
                    </div>
                    <div class="slide">
                        <img src="https://i.postimg.cc/667y71pV/IMG_7128.jpg" 
                             alt="Natural Ingredients" 
                             class="slide-img"
                             loading="lazy">
                        <div class="slide-caption">Natural Ingredients We Use</div>
                    </div>
                </div>
                
                <div class="gallery-controls">
                    <button class="prev-btn" id="prevBtn" aria-label="Previous slide">
                        <i class="fas fa-chevron-left"></i>
                    </button>
                    
                    <div class="gallery-dots" id="galleryDots" aria-label="Gallery navigation dots">
                        <!-- Dots will be generated by JavaScript -->
                    </div>
                    
                    <button class="next-btn" id="nextBtn" aria-label="Next slide">
                        <i class="fas fa-chevron-right"></i>
                    </button>
                </div>
            </div>
        </div>
    </section>
    
    <!-- FOOTER -->
    <footer id="contact">
        <div class="container">
            <div class="footer-content">
                <div class="footer-section">
                    <div class="footer-logo">BUSY BODY SOAPS</div>
                    <p>Handcrafted soap creations where natural ingredients meet artistic design.</p>
                    <div class="social-icons">
                        <a href="https://instagram.com/busysoap" target="_blank" aria-label="Instagram">
                            <i class="fab fa-instagram"></i>
                        </a>
                        <a href="#" target="_blank" aria-label="Etsy">
                            <i class="fab fa-etsy"></i>
                        </a>
                        <a href="#" target="_blank" aria-label="Email">
                            <i class="far fa-envelope"></i>
                        </a>
                    </div>
                </div>
                
                <div class="footer-section">
                    <h3>Quick Links</h3>
                    <ul class="footer-links">
                        <li><a href="#home" aria-label="Home">Home</a></li>
                        <li><a href="#products" aria-label="Products">Products</a></li>
                        <li><a href="#about" aria-label="About">About</a></li>
                        <li><a href="#values" aria-label="Values">Our Values</a></li>
                        <li><a href="#gallery" aria-label="Gallery">Gallery</a></li>
                    </ul>
                </div>
                
                <div class="footer-section">
                    <h3>Contact</h3>
                    <div class="contact-info">
                        <div><i class="fas fa-map-marker-alt"></i> <span>Mebi Kli Lopera Rd</span></div>
                        <div><i class="fas fa-phone"></i> <span>(215) 651-3763</span></div>
                    </div>
                </div>
            </div>
            
            <div class="copyright">
                <p>&copy; 2023 Busy Body Soaps. All rights reserved. <span class="highlight">CLEAN</span></p>
            </div>
        </div>
    </footer>
    
    <script>
        // ====================
        // ULTRA TECH SCRIPT - FIXED
        // ====================
        
        // DOM Ready
        document.addEventListener('DOMContentLoaded', function() {
            // Hide loading screen
            setTimeout(() => {
                const loading = document.getElementById('loading');
                if (loading) {
                    loading.style.opacity = '0';
                    setTimeout(() => {
                        loading.style.display = 'none';
                    }, 600);
                }
            }, 1500);
            
            // Initialize components
            initMobileMenu();
            initSmoothScroll();
            initGallery();
            initScrollEffects();
            initTechEffects();
            
            // Fix any visible code
            fixVisibleCode();
        });
        
        // FIX VISIBLE CODE ISSUE
        function fixVisibleCode() {
            // Check for any stray code elements
            const allElements = document.querySelectorAll('*');
            allElements.forEach(element => {
                if (element.textContent && element.textContent.includes('.value-card {')) {
                    element.style.display = 'none';
                }
            });
            
            // Make sure only proper elements are visible
            document.querySelectorAll('.value-card').forEach(card => {
                card.style.display = 'block';
                card.innerHTML = card.innerHTML.replace(/\.value-card\s*{/g, '');
            });
        }
        
        // MOBILE MENU - FIXED
        function initMobileMenu() {
            const mobileMenuBtn = document.getElementById('mobileMenuBtn');
            const navMenu = document.getElementById('navMenu');
            
            if (!mobileMenuBtn || !navMenu) return;
            
            const icon = mobileMenuBtn.querySelector('i');
            
            mobileMenuBtn.addEventListener('click', function() {
                navMenu.classList.toggle('active');
                document.body.style.overflow = navMenu.classList.contains('active') ? 'hidden' : 'auto';
                
                if (navMenu.classList.contains('active')) {
                    icon.classList.remove('fa-bars');
                    icon.classList.add('fa-times');
                    mobileMenuBtn.style.borderColor = 'var(--neon-orange)';
                    mobileMenuBtn.style.color = 'var(--neon-orange)';
                } else {
                    icon.classList.remove('fa-times');
                    icon.classList.add('fa-bars');
                    mobileMenuBtn.style.borderColor = 'var(--aqua-blue)';
                    mobileMenuBtn.style.color = 'var(--aqua-blue)';
                }
            });
            
            // Close menu on link click
            document.querySelectorAll('nav a').forEach(link => {
                link.addEventListener('click', () => {
                    navMenu.classList.remove('active');
                    document.body.style.overflow = 'auto';
                    if (icon) {
                        icon.classList.remove('fa-times');
                        icon.classList.add('fa-bars');
                    }
                    if (mobileMenuBtn) {
                        mobileMenuBtn.style.borderColor = 'var(--aqua-blue)';
                        mobileMenuBtn.style.color = 'var(--aqua-blue)';
                    }
                });
            });
            
            // Close menu on escape key
            document.addEventListener('keydown', (e) => {
                if (e.key === 'Escape' && navMenu.classList.contains('active')) {
                    navMenu.classList.remove('active');
                    document.body.style.overflow = 'auto';
                    if (icon) {
                        icon.classList.remove('fa-times');
                        icon.classList.add('fa-bars');
                    }
                    if (mobileMenuBtn) {
                        mobileMenuBtn.style.borderColor = 'var(--aqua-blue)';
                        mobileMenuBtn.style.color = 'var(--aqua-blue)';
                    }
                }
            });
        }
        
        // SMOOTH SCROLL - FIXED
        function initSmoothScroll() {
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function(e) {
                    const targetId = this.getAttribute('href');
                    if (targetId === '#' || !targetId) return;
                    
                    const targetElement = document.querySelector(targetId);
                    if (targetElement) {
                        e.preventDefault();
                        const headerHeight = document.querySelector('header').offsetHeight;
                        const targetPosition = targetElement.getBoundingClientRect().top + window.pageYOffset;
                        
                        window.scrollTo({
                            top: targetPosition - headerHeight,
                            behavior: 'smooth'
                        });
                        
                        // Close mobile menu if open
                        const navMenu = document.getElementById('navMenu');
                        const mobileMenuBtn = document.getElementById('mobileMenuBtn');
                        const icon = mobileMenuBtn ? mobileMenuBtn.querySelector('i') : null;
                        
                        if (navMenu && navMenu.classList.contains('active')) {
                            navMenu.classList.remove('active');
                            document.body.style.overflow = 'auto';
                            if (icon) {
                                icon.classList.remove('fa-times');
                                icon.classList.add('fa-bars');
                            }
                            if (mobileMenuBtn) {
                                mobileMenuBtn.style.borderColor = 'var(--aqua-blue)';
                                mobileMenuBtn.style.color = 'var(--aqua-blue)';
                            }
                        }
                    }
                });
            });
        }
        
        // GALLERY - FIXED
        function initGallery() {
            const slidesContainer = document.getElementById('slidesContainer');
            const prevBtn = document.getElementById('prevBtn');
            const nextBtn = document.getElementById('nextBtn');
            const galleryDots = document.getElementById('galleryDots');
            
            // Check if elements exist
            if (!slidesContainer || !galleryDots) return;
            
            let currentSlide = 0;
            const slides = document.querySelectorAll('.slide');
            const totalSlides = slides.length;
            
            if (totalSlides === 0) return;
            
            let autoSlideInterval;
            let isTransitioning = false;
            
            // Create dots
            galleryDots.innerHTML = '';
            for (let i = 0; i < totalSlides; i++) {
                const dot = document.createElement('button');
                dot.classList.add('dot');
                dot.setAttribute('aria-label', `Go to slide ${i + 1}`);
                dot.setAttribute('type', 'button');
                if (i === 0) dot.classList.add('active');
                dot.addEventListener('click', () => goToSlide(i));
                galleryDots.appendChild(dot);
            }
            
            // Go to slide function
            function goToSlide(slideIndex) {
                if (isTransitioning || slideIndex === currentSlide) return;
                
                isTransitioning = true;
                
                if (slideIndex < 0) slideIndex = totalSlides - 1;
                if (slideIndex >= totalSlides) slideIndex = 0;
                
                currentSlide = slideIndex;
                slidesContainer.style.transform = `translateX(-${currentSlide * 100}%)`;
                
                // Update dots
                document.querySelectorAll('.dot').forEach((dot, index) => {
                    dot.classList.toggle('active', index === currentSlide);
                });
                
                // Reset auto slide timer
                resetAutoSlide();
                
                // Allow next transition after animation completes
                setTimeout(() => {
                    isTransitioning = false;
                }, 600);
            }
            
            // Navigation
            if (prevBtn) {
                prevBtn.addEventListener('click', () => {
                    if (isTransitioning) return;
                    goToSlide(currentSlide - 1);
                    // Button effect
                    prevBtn.style.animation = 'controlHoverTech 0.4s steps(1, end)';
                    setTimeout(() => {
                        prevBtn.style.animation = '';
                    }, 400);
                });
            }
            
            if (nextBtn) {
                nextBtn.addEventListener('click', () => {
                    if (isTransitioning) return;
                    goToSlide(currentSlide + 1);
                    // Button effect
                    nextBtn.style.animation = 'controlHoverTech 0.4s steps(1, end)';
                    setTimeout(() => {
                        nextBtn.style.animation = '';
                    }, 400);
                });
            }
            
            // Auto slide
            function startAutoSlide() {
                if (totalSlides > 1) {
                    autoSlideInterval = setInterval(() => {
                        if (!isTransitioning) {
                            goToSlide(currentSlide + 1);
                        }
                    }, 5000);
                }
            }
            
            function resetAutoSlide() {
                clearInterval(autoSlideInterval);
                startAutoSlide();
            }
            
            // Pause on hover
            const gallery = document.querySelector('.slide-gallery');
            if (gallery && totalSlides > 1) {
                gallery.addEventListener('mouseenter', () => clearInterval(autoSlideInterval));
                gallery.addEventListener('mouseleave', startAutoSlide);
                
                // Also pause on touch devices
                gallery.addEventListener('touchstart', () => clearInterval(autoSlideInterval));
                gallery.addEventListener('touchend', () => setTimeout(startAutoSlide, 3000));
            }
            
            // Start auto slide
            startAutoSlide();
            
            // Make gallery dots keyboard accessible
            galleryDots.querySelectorAll('.dot').forEach((dot, index) => {
                dot.addEventListener('keydown', (e) => {
                    if (e.key === 'Enter' || e.key === ' ') {
                        e.preventDefault();
                        goToSlide(index);
                    }
                });
            });
        }
        
        // SCROLL EFFECTS - FIXED
        function initScrollEffects() {
            let ticking = false;
            const header = document.querySelector('header');
            
            if (!header) return;
            
            window.addEventListener('scroll', () => {
                if (!ticking) {
                    window.requestAnimationFrame(() => {
                        const scrolled = window.pageYOffset;
                        
                        if (scrolled > 100) {
                            header.style.backgroundColor = 'rgba(10, 14, 42, 0.98)';
                            header.style.backdropFilter = 'blur(20px)';
                        } else {
                            header.style.backgroundColor = 'rgba(10, 14, 42, 0.95)';
                            header.style.backdropFilter = 'blur(15px)';
                        }
                        
                        ticking = false;
                    });
                    
                    ticking = true;
                }
            });
        }
        
        // TECH EFFECTS - OPTIMIZED FOR MOBILE
        function initTechEffects() {
            // Only run heavy animations if not on mobile
            if (window.innerWidth < 768) return;
            
            // Random tech effect on logo
            setInterval(() => {
                const logo = document.querySelector('.logo-text');
                if (!logo) return;
                
                const originalAnimation = logo.style.animation;
                
                // Trigger digital effect
                logo.style.color = 'var(--aqua-blue)';
                logo.style.textShadow = 'var(--glow-aqua)';
                logo.style.animation = 'hoverTech 1s steps(1, end)';
                
                setTimeout(() => {
                    logo.style.color = '';
                    logo.style.textShadow = '';
                    logo.style.animation = originalAnimation || 'techPulse 3s ease-in-out infinite, digitalShake 10s ease-in-out infinite';
                }, 1000);
                
            }, Math.random() * 10000 + 10000); // 10-20 seconds
            
            // Random tech effect
            setInterval(() => {
                const techElements = document.querySelectorAll('.hero h1, .section-title');
                if (techElements.length === 0) return;
                
                const randomElement = techElements[Math.floor(Math.random() * techElements.length)];
                const originalColor = randomElement.style.color;
                
                randomElement.style.color = 'var(--aqua-blue)';
                randomElement.style.textShadow = '0 0 40px var(--aqua-blue)';
                
                setTimeout(() => {
                    randomElement.style.color = originalColor;
                    randomElement.style.textShadow = '';
                }, 500);
                
            }, Math.random() * 8000 + 5000); // 5-13 seconds
            
            // Tech scan effect on cards
            setInterval(() => {
                const cards = document.querySelectorAll('.product-card, .value-card');
                if (cards.length === 0) return;
                
                const randomCard = cards[Math.floor(Math.random() * cards.length)];
                
                if (randomCard) {
                    const originalBorder = randomCard.style.borderColor;
                    randomCard.style.borderColor = 'var(--neon-orange)';
                    randomCard.style.boxShadow = 'var(--glow-orange)';
                    
                    setTimeout(() => {
                        randomCard.style.borderColor = originalBorder;
                        randomCard.style.boxShadow = '';
                    }, 800);
                }
            }, Math.random() * 6000 + 4000); // 4-10 seconds
        }
        
        // Handle window resize
        window.addEventListener('resize', function() {
            // Re-initialize gallery if needed
            const galleryDots = document.getElementById('galleryDots');
            if (galleryDots && galleryDots.children.length === 0) {
                initGallery();
            }
        });
        
        // Improve mobile touch interactions
        document.addEventListener('touchstart', function() {}, {passive: true});
    </script>
</body>
</html>
